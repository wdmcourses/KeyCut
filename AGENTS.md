* Никаких комментариев в коде
* Финальная: KeyCut-win-x64, KeyCut-linux-x64, KeyCut-mac-arm64
* Без описания в релизах
* Алгоритм склейки частей без потери кадров: ffmpeg concat demuxer в MKV-контейнер (`-f concat -safe 0 -i list.txt -c copy out.mkv`), НЕ в MP4/MOV — H.264 с B-кадрами даёт отрицательный DTS у первого кадра второго файла, MP4/MOV его теряет, MKV сохраняет (пересчитывает DTS). Проверено: 141+129 кадров → ровно 270 в MKV, стык чистый.
